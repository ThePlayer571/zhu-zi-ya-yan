# 待办


## 闯关模式编辑器bug

编辑器右下可以选择查看 交互台/经注疏/结果，这是三个窗口，出现在同一位置。
但是，当这三个窗口内存在内容时，它们对应的内容的高度不一致，因此切换时会出现跳一下。

修复这个问题。它们对应的窗口高度应该始终一致。

这个可能是窗口位置的问题，我看到经注疏窗口往下位移了一段。

你应该确保这三个窗口的顶部位置始终不变，允许往下延伸以显示更多内容。

## 闯关模式编辑器运行功能体验优化

目前的运行按钮，点击后不会提供任意输入。用户想要查看有输入的运行结果就必须要提交。
这显然不好。
你需要让用户能自己编辑输入内容（在交互台页面）。这会是一个文本框（而不是主界面编辑器那种即时交互式的）。多个输入使用“、”分隔。

## 关卡风格问题

有些关卡描述中出现了类似"n"、"k"这样的现代符号。你应该把它们改成古代汉语的表达方式，比如用甲乙丙代替。

当然，总体是以描述清楚为最高级别要求。nk是由于太出戏才必须更换。

## 闯关模式提示显示不一致问题

现在提示“是否显示”是所有关卡共通的。我希望你改成：切换/退出关卡时，自动隐藏提示，防止用户被提示干扰。